onboarding = false
# Mirrors the Omarchy tmux config in config/tmux/tmux.conf
# tmux session -> herdr workspace, tmux window -> herdr tab, tmux pane -> herdr pane

[theme]
# tmux ran on the terminal's own palette (bg=default, fg=default, ANSI blue accents)
name = "terminal"

[theme.custom]
# The active tab is drawn as panel_bg text on an accent background, so panel_bg
# has to be dark for it to read - same colors as status-left's "#[fg=black,bg=blue]"
panel_bg = "black"

[terminal]
# Left unset, herdr falls back to $SHELL, which is whatever shell happened to
# start the server - bash, when it came up inside tmux - and not the login shell
# from /etc/passwd. Naming fish here pins it no matter how the server was
# launched. A bare name is resolved on PATH, so this also holds on machines
# where Homebrew owns fish.
default_shell = "fish"

# Matches -c "#{pane_current_path}" on every split, window, and session
new_cwd = "follow"

[keys]
prefix = "ctrl+b"

# Config and help
reload_config = "prefix+q"
help = "prefix+?"
detach = "prefix+d"

# Copy mode
copy_mode = "prefix+["

# Panes
split_horizontal = ["prefix+h", 'prefix+"', "alt+enter"]
split_vertical = ["prefix+v", "prefix+%", "alt+shift+enter"]
close_pane = ["prefix+x", "alt+esc"]
zoom = "prefix+z"
last_pane = "prefix+;"

focus_pane_left = "ctrl+alt+left"
focus_pane_down = "ctrl+alt+down"
focus_pane_up = "ctrl+alt+up"
focus_pane_right = "ctrl+alt+right"

resize_mode = ["prefix+ctrl+left", "prefix+ctrl+down", "prefix+ctrl+up", "prefix+ctrl+right"]

# Like resize-pane on C-M-S-arrows
resize_pane_left = "ctrl+alt+shift+left"
resize_pane_down = "ctrl+alt+shift+down"
resize_pane_up = "ctrl+alt+shift+up"
resize_pane_right = "ctrl+alt+shift+right"

# No tmux equivalent; herdr's default prefix+shift+p is taken by previous session
rename_pane = "prefix+shift+o"

# Windows -> tabs
new_tab = "prefix+c"
rename_tab = "prefix+r"
close_tab = "prefix+k"
switch_tab = ["prefix+1..9", "alt+1..9"]
previous_tab = ["prefix+p", "alt+left"]
next_tab = ["prefix+n", "alt+right"]

# Like swap-window -t -1/+1 on M-S-Left/Right
move_tab_previous = "alt+shift+left"
move_tab_next = "alt+shift+right"

# Sessions -> workspaces
new_workspace = "prefix+shift+c"
rename_workspace = "prefix+shift+r"
close_workspace = "prefix+shift+k"
previous_workspace = ["prefix+shift+p", "alt+up"]
next_workspace = ["prefix+shift+n", "alt+down"]

[ui]
accent = "blue"

# tmux drew single-line dividers between adjacent panes and no outer frame
pane_gaps = false
pane_outer_borders = false

# tmux had no scrollbar column beside its panes
pane_scrollbars = false

# kill-window and kill-session never asked
confirm_close = false

# automatic-rename gave windows a name without prompting
prompt_new_tab_name = false

# set -g mouse on
mouse_capture = true

# Herdr's tab row doubles as its tmux-style status bar. The Clockify command
# redraws elapsed time every second, while the script caches API calls for 60s.
tab_bar_right = [
  { type = "zoom" },
  { type = "command", command = "~/.local/bin/herdr-clockify-status", interval_seconds = 1, timeout_seconds = 30 },
  { type = "command", command = "~/.local/bin/herdr-kube-status", interval_seconds = 5, timeout_seconds = 2 },
  { type = "hostname" },
  # tmux had no clock; the datetime entry is a herdr built-in that redraws on
  # the server, so it costs nothing next to the command entries above.
  { type = "datetime", format = "%H:%M" },
]
tab_bar_right_separator = " · "

# set -g set-titles on / set -g set-titles-string '#h:#W', where tmux's #W was
# the basename of the pane cwd. This is what Hyprland shows in the group bar,
# and it resolves on the server so remote sessions name the remote host.
window_title = "{hostname}: {workspace}"
agent_panel_sort = "spaces"

show_agent_labels_on_pane_borders = true
[ui.toast]
delivery = "system"

# herdr has no URL-hint action - its keys stop at copy_mode and edit_scrollback -
# and Alacritty's CTRL+SHIFT+U hints read only the visible grid, so a URL cut by
# a pane border or scrolled off is out of reach there. herdr-url-pick asks the
# server for the pane's unwrapped scrollback instead and pipes the matches
# through fzf.
[[keys.command]]
key = "prefix+alt+u"
type = "popup"
command = "herdr-url-pick"
width = "70%"
height = "50%"
